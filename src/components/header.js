import "../styles/header.css";

const Header = () => {
  return (
    <header id="main-header">
      <div id="logo">
        <h1>Pixel Renaissance</h1>
      </div>
    </header>
  );
};

export default Header;
